// Code generated by smithy-swift-codegen. DO NOT EDIT!

import ClientRuntime

extension EC2ClientTypes.VolumeStatusItem: Swift.Codable {
    enum CodingKeys: Swift.String, Swift.CodingKey {
        case actions = "actionsSet"
        case attachmentStatuses = "attachmentStatuses"
        case availabilityZone = "availabilityZone"
        case events = "eventsSet"
        case outpostArn = "outpostArn"
        case volumeId = "volumeId"
        case volumeStatus = "volumeStatus"
    }

    public func encode(to encoder: Swift.Encoder) throws {
        var container = encoder.container(keyedBy: ClientRuntime.Key.self)
        if let actions = actions {
            if !actions.isEmpty {
                for (index0, volumestatusaction0) in actions.enumerated() {
                    var actionsContainer0 = container.nestedContainer(keyedBy: ClientRuntime.Key.self, forKey: ClientRuntime.Key("ActionsSet.\(index0.advanced(by: 1))"))
                    try actionsContainer0.encode(volumestatusaction0, forKey: ClientRuntime.Key(""))
                }
            }
            else {
                var actionsContainer = container.nestedContainer(keyedBy: ClientRuntime.Key.self, forKey: ClientRuntime.Key("ActionsSet"))
                try actionsContainer.encode("", forKey: ClientRuntime.Key(""))
            }
        }
        if let attachmentStatuses = attachmentStatuses {
            if !attachmentStatuses.isEmpty {
                for (index0, volumestatusattachmentstatus0) in attachmentStatuses.enumerated() {
                    var attachmentStatusesContainer0 = container.nestedContainer(keyedBy: ClientRuntime.Key.self, forKey: ClientRuntime.Key("AttachmentStatuses.\(index0.advanced(by: 1))"))
                    try attachmentStatusesContainer0.encode(volumestatusattachmentstatus0, forKey: ClientRuntime.Key(""))
                }
            }
            else {
                var attachmentStatusesContainer = container.nestedContainer(keyedBy: ClientRuntime.Key.self, forKey: ClientRuntime.Key("AttachmentStatuses"))
                try attachmentStatusesContainer.encode("", forKey: ClientRuntime.Key(""))
            }
        }
        if let availabilityZone = availabilityZone {
            try container.encode(availabilityZone, forKey: ClientRuntime.Key("AvailabilityZone"))
        }
        if let events = events {
            if !events.isEmpty {
                for (index0, volumestatusevent0) in events.enumerated() {
                    var eventsContainer0 = container.nestedContainer(keyedBy: ClientRuntime.Key.self, forKey: ClientRuntime.Key("EventsSet.\(index0.advanced(by: 1))"))
                    try eventsContainer0.encode(volumestatusevent0, forKey: ClientRuntime.Key(""))
                }
            }
            else {
                var eventsContainer = container.nestedContainer(keyedBy: ClientRuntime.Key.self, forKey: ClientRuntime.Key("EventsSet"))
                try eventsContainer.encode("", forKey: ClientRuntime.Key(""))
            }
        }
        if let outpostArn = outpostArn {
            try container.encode(outpostArn, forKey: ClientRuntime.Key("OutpostArn"))
        }
        if let volumeId = volumeId {
            try container.encode(volumeId, forKey: ClientRuntime.Key("VolumeId"))
        }
        if let volumeStatus = volumeStatus {
            try container.encode(volumeStatus, forKey: ClientRuntime.Key("VolumeStatus"))
        }
    }

    public init (from decoder: Swift.Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        if containerValues.contains(.actions) {
            struct KeyVal0{struct item{}}
            let actionsWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .actions)
            if let actionsWrappedContainer = actionsWrappedContainer {
                let actionsContainer = try actionsWrappedContainer.decodeIfPresent([EC2ClientTypes.VolumeStatusAction].self, forKey: .member)
                var actionsBuffer:[EC2ClientTypes.VolumeStatusAction]? = nil
                if let actionsContainer = actionsContainer {
                    actionsBuffer = [EC2ClientTypes.VolumeStatusAction]()
                    for structureContainer0 in actionsContainer {
                        actionsBuffer?.append(structureContainer0)
                    }
                }
                actions = actionsBuffer
            } else {
                actions = []
            }
        } else {
            actions = nil
        }
        let availabilityZoneDecoded = try containerValues.decodeIfPresent(Swift.String.self, forKey: .availabilityZone)
        availabilityZone = availabilityZoneDecoded
        let outpostArnDecoded = try containerValues.decodeIfPresent(Swift.String.self, forKey: .outpostArn)
        outpostArn = outpostArnDecoded
        if containerValues.contains(.events) {
            struct KeyVal0{struct item{}}
            let eventsWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .events)
            if let eventsWrappedContainer = eventsWrappedContainer {
                let eventsContainer = try eventsWrappedContainer.decodeIfPresent([EC2ClientTypes.VolumeStatusEvent].self, forKey: .member)
                var eventsBuffer:[EC2ClientTypes.VolumeStatusEvent]? = nil
                if let eventsContainer = eventsContainer {
                    eventsBuffer = [EC2ClientTypes.VolumeStatusEvent]()
                    for structureContainer0 in eventsContainer {
                        eventsBuffer?.append(structureContainer0)
                    }
                }
                events = eventsBuffer
            } else {
                events = []
            }
        } else {
            events = nil
        }
        let volumeIdDecoded = try containerValues.decodeIfPresent(Swift.String.self, forKey: .volumeId)
        volumeId = volumeIdDecoded
        let volumeStatusDecoded = try containerValues.decodeIfPresent(EC2ClientTypes.VolumeStatusInfo.self, forKey: .volumeStatus)
        volumeStatus = volumeStatusDecoded
        if containerValues.contains(.attachmentStatuses) {
            struct KeyVal0{struct item{}}
            let attachmentStatusesWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .attachmentStatuses)
            if let attachmentStatusesWrappedContainer = attachmentStatusesWrappedContainer {
                let attachmentStatusesContainer = try attachmentStatusesWrappedContainer.decodeIfPresent([EC2ClientTypes.VolumeStatusAttachmentStatus].self, forKey: .member)
                var attachmentStatusesBuffer:[EC2ClientTypes.VolumeStatusAttachmentStatus]? = nil
                if let attachmentStatusesContainer = attachmentStatusesContainer {
                    attachmentStatusesBuffer = [EC2ClientTypes.VolumeStatusAttachmentStatus]()
                    for structureContainer0 in attachmentStatusesContainer {
                        attachmentStatusesBuffer?.append(structureContainer0)
                    }
                }
                attachmentStatuses = attachmentStatusesBuffer
            } else {
                attachmentStatuses = []
            }
        } else {
            attachmentStatuses = nil
        }
    }
}
