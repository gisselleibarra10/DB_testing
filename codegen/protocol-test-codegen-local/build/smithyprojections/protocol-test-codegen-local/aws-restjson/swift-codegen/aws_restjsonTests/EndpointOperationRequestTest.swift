// Code generated by smithy-swift-codegen. DO NOT EDIT!

@testable import aws_restjson
import AWSClientRuntime
import ClientRuntime
import SmithyTestUtil
import XCTest


class EndpointOperationRequestTest: HttpRequestTestBase {
    /// Operations can prepend to the given host if they define the endpoint trait.
    func testRestJsonEndpointTrait() async throws {
        let urlPrefix = urlPrefixFromHost(host: "example.com")
        let hostOnly = hostOnlyFromHost(host: "example.com")
        let expected = buildExpectedHttpRequest(
            method: .post,
            path: "/EndpointOperation",
            body: nil,
            host: "example.com",
            resolvedHost: "foo.example.com"
        )

        let decoder = ClientRuntime.JSONDecoder()
        decoder.dateDecodingStrategy = .secondsSince1970
        decoder.nonConformingFloatDecodingStrategy = .convertFromString(positiveInfinity: "Infinity", negativeInfinity: "-Infinity", nan: "NaN")

        let input = EndpointOperationInput(
        )
        let encoder = ClientRuntime.JSONEncoder()
        encoder.dateEncodingStrategy = .secondsSince1970
        encoder.nonConformingFloatEncodingStrategy = .convertToString(positiveInfinity: "Infinity", negativeInfinity: "-Infinity", nan: "NaN")
        let context = HttpContextBuilder()
                      .withEncoder(value: encoder)
                      .withMethod(value: .post)
                      .build()
        var operationStack = OperationStack<EndpointOperationInput, EndpointOperationOutputResponse, EndpointOperationOutputError>(id: "RestJsonEndpointTrait")
        operationStack.initializeStep.intercept(position: .after, middleware: ClientRuntime.URLPathMiddleware<EndpointOperationInput, EndpointOperationOutputResponse, EndpointOperationOutputError>(urlPrefix: urlPrefix))
        operationStack.initializeStep.intercept(position: .after, middleware: ClientRuntime.URLHostMiddleware<EndpointOperationInput, EndpointOperationOutputResponse>(host: hostOnly, hostPrefix: "foo."))
        operationStack.buildStep.intercept(position: .after, id: "RequestTestEndpointResolver") { (context, input, next) -> ClientRuntime.OperationOutput<EndpointOperationOutputResponse> in
            input.withMethod(context.getMethod())
            input.withPath(context.getPath())
            let host = "\(context.getHostPrefix() ?? "")\(context.getHost() ?? "")"
            input.withHost(host)
            return try await next.handle(context: context, input: input)
        }
        operationStack.deserializeStep.intercept(position: .after,
                     middleware: MockDeserializeMiddleware<EndpointOperationOutputResponse, EndpointOperationOutputError>(
                             id: "TestDeserializeMiddleware"){ context, actual in
            self.assertEqual(expected, actual)
            let response = HttpResponse(body: HttpBody.none, statusCode: .ok)
            let mockOutput = try! EndpointOperationOutputResponse(httpResponse: response, decoder: nil)
            let output = OperationOutput<EndpointOperationOutputResponse>(httpResponse: response, output: mockOutput)
            return output
        })
        _ = try await operationStack.handleMiddleware(context: context, input: input, next: MockHandler(){ (context, request) in
            XCTFail("Deserialize was mocked out, this should fail")
            let httpResponse = HttpResponse(body: .none, statusCode: .badRequest)
            let serviceError = try! EndpointOperationOutputError(httpResponse: httpResponse)
            throw SdkError<EndpointOperationOutputError>.service(serviceError, httpResponse)
        })
    }
}
